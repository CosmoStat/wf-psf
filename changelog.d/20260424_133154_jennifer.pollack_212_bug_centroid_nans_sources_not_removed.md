<!--
A new scriv changelog fragment.

Uncomment the section that is right (remove the HTML comment wrapper).
For top level release notes, leave all the headers commented out.
-->

<!--
### Breaking changes

- A bullet item for the Breaking changes category.

-->

### New features

- Added `safe_batch` utilities for robust filtering of aligned datasets based on sample-wise validity masks (e.g. handling NaNs/Infs in centroid arrays).
- Introduced `safe_batch_builder` to ensure consistent filtering across multiple aligned arrays while preserving dataset integrity.
- Added support for filtering both NumPy arrays and sequence-based metadata (e.g. object IDs) based on mask alignment.
- Included logging utilities to track and debug filtered samples.

<!--
### Bug fixes

- A bullet item for the Bug fixes category.

-->
<!--
### Performance improvements

- A bullet item for the Performance improvements category.

-->

### Internal changes

- Added comprehensive unit tests covering edge cases such as partial invalid samples, fully invalid batches, and misaligned inputs.
- Enforced stricter validation to prevent silent misalignment (e.g. raising on inconsistent array lengths or fully invalid batches).
- Improved handling of array-like inputs and clarified expected behavior for aligned vs non-aligned data.

